# ClawSearch Environment Configuration
# Copy this file to .env and fill in your actual values

# SearXNG Configuration
SEARXNG_SECRET_KEY=your_random_secret_key_here_32_chars_min
SEARXNG_BASE_URL=https://your-domain.com/

# SSL Configuration (optional - for HTTPS)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/private.key

# Network Configuration
SEARXNG_PORT=8080
NGINX_PORT=80
NGINX_SSL_PORT=443

# Redis Configuration
REDIS_PASSWORD=your_redis_password_here

# Additional Security (optional)
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Instructions:
# 1. Generate SEARXNG_SECRET_KEY: openssl rand -hex 32
# 2. Generate REDIS_PASSWORD: openssl rand -base64 32
# 3. Set SEARXNG_BASE_URL to your actual domain
# 4. Configure SSL paths if using HTTPS 